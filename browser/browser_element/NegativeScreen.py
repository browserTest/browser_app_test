"""
负一屏相关常量
"""
# 负一屏的添加按钮 —— LJX
NEGATIVE_ADD_TEXT = '添加'

# 长按负一屏书签，弹框的删除按钮 —— LJX
DELETE_TEXT = '删除'

# 长按负一屏书签，弹框的新窗口打开按钮 —— LJX
NEW_WINDOW_OPEN = '新窗口打开'

# 长按负一屏书签，弹框的编辑按钮 —— LJX
EDIT = '编辑'

# 长按负一屏书签，弹框的发送至桌面按钮 —— LJX
ADD_TO_DESK = '发送至桌面'

# 长按负一屏书签，弹框的多选按钮 —— LJX
SELECT_MORE = '多选'

# 负一屏书签编辑页面的网址URL的ID —— LJX
WEBSITE_URL = 'com.android.browser:id/address'

# 负一屏书签编辑页面的网址名称ID —— LJX
WEBSITE_NAME = 'com.android.browser:id/title'

# 负一屏书签编辑页面的确认按钮ID —— LJX
EDIT_CONFIRM = 'com.android.browser:id/qr_code'

# 负一屏长按书签选择多选后，顶部中间的“已选择N项”ID —— LJX
ACTION_BAR = 'com.android.browser:id/action_bar_title'

# 负一屏长按书签选择多选后，右上角的“全选”/“全不选”ID —— LJX
SELECT = 'com.android.browser:id/mz_action_multi_choice_select_all_item'

# 负一屏长按书签选择多选后，左上角的“取消”ID —— LJX
SELECT_CANCEL = 'com.android.browser:id/mz_action_multi_choice_close_item'

# “自动化测试”文件夹 —— LJX
AUTOMATION_FILE = '自动化测试'

# 长按文件夹弹框的“删除文件夹” —— LJX
DELETE_FILE = '删除文件夹'

# 删除文件夹的“确认”按钮 —— LJX
CONFIRM_TEXT = '确定'

# 添加收藏-编辑-确认按钮ID —— LJX
CONFIRM_ID = 'com.android.browser:id/title'

# 文件夹 —— LJX
FILE = '文件夹'

# 重命名 —— LJX
RERAME_FILE = '重命名'

# 重命名文件夹文本输入框 —— LJX
RERAME_FILE_ID = 'com.android.browser:id/name'

# 指定坐标 —— LJX
DRAG_FILE_POSITION = (0.495, 0.227)

# 负一屏-》已保存网页 —— LJX
SAVED_PAGE = '已保存网页'

# 负一屏任意书签图标ID —— LJX
BOOKMARK_ICON = 'com.android.browser:id/fragment_shotcut'








